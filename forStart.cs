using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class forStart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Time.timeScale = 1;
        DialogueTrigger.andar = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
